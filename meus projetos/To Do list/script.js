//constantes e seletores
const area= document.querySelector(".area textarea"),
todo=document.querySelector(".todoLists"),
numPendente= document.querySelector(".pendenteNum"),
limpar=document.querySelector(".limpar");

//vamos fazer a função para adicionar, limpar e confirmar a tarefa

function{
    let task = document.querySelectorAll(".pendente");
    numPendente.textContent = task.length === 0 ? "no" : task.length;
    let allList = 
}